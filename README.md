# style: sx props, styled api, theme override
# sx style: one time styling
# style api: Create a styled component with the styled API if you need multiple components of the same type to have the same styling